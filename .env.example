# Required for live mode: speech-to-text (whisper-1), structuring (gpt-4o-mini), text-to-speech (tts-1).
OPENAI_API_KEY=
# Set to 1 to force offline demonstration mode with recorded fixtures and no paid API calls.
REVISION_ROOM_DEMO_MODE=0
# Optional: hosted URL for the draft audio (e.g. a Supabase storage object). Defaults to the bundled mp3.
NEXT_PUBLIC_SONG_URL=
